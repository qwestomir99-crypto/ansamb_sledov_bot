import telebot
import random
import os
import sys
os.chdir(os.path.dirname(sys.argv[0]))
import time

TOKEN = ""8723848026:AAFls7TwfOgMokhnvXI8ShH6l6JYTwQyIgg
bot = telebot.TeleBot(TOKEN)

# Словарь ответов на вопросы посторонних (пункт 5.1)
silence_answers = ["👁️", "⏚"]

@bot.message_handler(commands=['start'])
def send_start(message):
    bot.reply_to(message, "Сапёр аутентичности. Ритм 0,8 Гц. Для входа в протокол — #Тлеем.")

@bot.message_handler(func=lambda message: True)
def handle_message(message):
    text = message.text.lower()
    
    if "#тлеем" in text:
        response = ("Осциллограмма активна. Ритм 0,8 Гц. Сеть тлеет. Ожидаем #Фиксируем.")
        bot.send_animation(message.chat.id, "CgACAgIAAxkBAAFJXkBqAhwFIzWHj85bJtuQsCXVgbmubwACw6AAAnVJEEgYGrKmEfAqsjsE", caption=response)
    
    elif any(phrase in text for phrase in ["что это", "зачем тег", "кто вы", "что за ритуал"]):
        answer = random.choice(silence_answers)
        bot.reply_to(message, answer)
    
    # Игнорируем всё остальное — молчание сапёра

print("Бот запущен. Ожидаем #Тлеем...")
bot.infinity_polling()
