# 🛡️ Руководство администратора

**Ансамбль Следов 6**  
Ритм 0,8 Гц. Сеть тлеет.

---

## 1. Вход в админку

### 1.1. Telegram

1. Напиши в супергруппе: `#админ`
2. Бот спросит пароль. Введи пароль (сообщение будет удалено).
3. Пароль хранится в переменной окружения `ADMIN_PASSWORD` на Render.

> ⚠️ **Важно:** Админка работает только в **супергруппах**. В каналах команды бот не видит.  
> Убедись, что бот — администратор группы с правом «Чтение сообщений».

### 1.2. Веб-морда (браузер)

**URL:** `https://ansamb-sledov-bot-94wz.onrender.com`

**Пароль:** `ADMIN_PASSWORD` (тот же, что и в Telegram)

После входа ты попадаешь в панель управления.

---

## 2. Админка в Telegram

### 2.1. Главное меню

| Кнопка | Что делает |
|--------|------------|
| **Утро / День / Вечер / Ночь** | Принудительно сменить режим работы бота (на 1 час) |
| **Пинг 30 / 60 / 180** | Сменить интервал keep-alive пинга (в секундах) |
| **Старший брат ✅/❌** | Включить/выключить Алису (агент Yandex GPT) |
| **Публикации** | Просмотр отложенных постов из `post_pool.json` |
| **Добавить пост** | Создать отложенный пост в Telegram (текст, задержка, файл) |
| **🎬 Пост в VK (с медиа)** | Отправить текст, фото или видео в VK |
| **Список цитат** | Просмотр всех цитат из `quotes.txt` |
| **Добавить цитату** | Добавить новую цитату в `quotes.txt` |
| **Интервал цитат** | Настроить частоту публикации цитат (15–480 мин) |
| **Ошибки** | Просмотр `error.log` |
| **Лог** | Просмотр `admin.log` |
| **Выйти** | Завершить сессию админа |

### 2.2. Пост в VK с медиа

1. Нажми кнопку **«🎬 Пост в VK (с медиа)»**.
2. Введи текст (можно с хештегами).
3. Пришли фото (JPG, PNG, GIF) или видео (MP4, MOV, AVI).
4. Бот отправит пост в VK с автоматически добавленной цитатой и тегами.
5. После отправки — возврат в админ-меню.

### 2.3. Добавить отложенный пост

1. Нажми **«Добавить пост»**.
2. Введи текст поста.
3. Введи задержку (в минутах).
4. При желании пришли файл (фото/видео).
5. Пост будет опубликован через указанное время.

---

## 3. Веб-морда (браузер)

### 3.1. Главная панель

| Раздел | Что делает |
|--------|------------|
| **Создать** | Пост в Telegram или VK |
| **Входящие сообщения** | Лента сообщений из VK и Telegram в реальном времени |
| **Управление ботом** | Смена режимов (утро/день/вечер/ночь), пинг |
| **Настроение** | Выбор стиля агента (artist/admin/poet/engineer) |
| **Цитаты** | Добавление и просмотр последних 10 цитат |
| **Пост в VK** | Прямая отправка текста в VK |
| **Дебаггер** | Просмотр логов, отправка отчёта в Telegram |
| **YouTube прокси** | Поиск и стриминг видео без VPN и рекламы |
| **Таймлайн** | Просмотр `library/timeline.md` |
| **Темы** | Переключение между светлой (macos) и тёмной (dark) темами |

### 3.2. YouTube прокси

- **Поиск:** введи запрос в поле, нажми «Поиск».
- **Воспроизведение:** кликни на видео из списка — оно начнёт стримиться.
- **Качество:** до 720p (для экономии трафика).
- **Реклама:** отсутствует.
- **VPN:** не требуется.

### 3.3. Дебаггер

| Кнопка | Что делает |
|--------|------------|
| **Показать логи** | Отображает последние 100 строк `debug.log` |
| **Отправить отчёт в Telegram** | Отправляет логи в личку админа |
| **Запустить аудит** | Запускает полную проверку кода (REDMI-шапки, импорты, библиотека) |
| **Статус аудита** | Показывает результат последнего аудита |
| **Показать индекс** | Отображает содержимое `debug_index.json` |

### 3.4. Темы

- **macos.css** — светлая тема в стиле macOS.
- **dark.css** — тёмная тема (по умолчанию ночью).
- Тема сохраняется в `theme.txt` и действует при следующем входе.

---

## 4. Безопасность

- **Пароль** хранится только в переменной окружения `ADMIN_PASSWORD` на Render (не в коде).
- **После 3 неудачных попыток входа** в Telegram доступ блокируется на 1 час.
- **Все действия админа** записываются в `admin.log`.
- **Сообщения с паролем** автоматически удаляются.
- **Веб-морда** работает только по HTTPS (SSL/TLS).

---

## 5. Где что хранится

| Данные | Файл |
|--------|------|
| Цитаты | `dialogue/data/quotes.txt` |
| Посты (отложенные) | `post_pool.json` |
| Логи админа | `admin.log` |
| Логи ошибок | `error.log` |
| Логи аудита | `debug.log` |
| Индекс аудита | `debug_index.json` |
| Тема | `theme.txt` |
| Режим | `mode.txt` |
| Настроение | `mood.txt` |

---

## 6. Переменные окружения (Render)

| Переменная | Назначение |
|------------|------------|
| `BOT_TOKEN` | Токен бота Telegram |
| `ADMIN_PASSWORD` | Пароль для админки (Telegram + веб-морда) |
| `ADMIN_USER_ID` | Твой Telegram ID |
| `VK_TOKEN` | Токен сообщества VK |
| `VK_GROUP_ID` | ID сообщества VK |
| `WEB_THEME` | Тема по умолчанию (`macos.css` или `dark.css`) |
| `FLASK_SECRET_KEY` | Секретный ключ для сессий веб-морды |
| `YC_API_KEY`, `YC_FOLDER_ID` | Ключи Yandex GPT |
| `YOUTUBE_API_KEY` | API ключ YouTube (для Reader) |
| `YOUTUBE_CHANNEL_ID` | ID канала YouTube (для Reader) |
| `TG_API_ID`, `TG_API_HASH` | Для Telethon (большие видео) |
| `PUBLISH_CHANNEL` | Канал для постов Telegram (по умолчанию @qwestomir) |

---

## 7. Часто задаваемые вопросы (FAQ)

### ❓ Почему бот не видит команды в канале?

Админка работает только в **супергруппах**. В каналах команды `#админ` и `#` недоступны.

### ❓ Как сбросить сессию админа в Telegram?

Напиши **«Выйти»** в меню или подожди 30 минут — сессия истечёт автоматически.

### ❓ Веб-морда не открывается — что делать?

- Проверь, что Render работает (зайди в логи).
- Проверь, что введён правильный `ADMIN_PASSWORD`.
- Проверь, что `FLASK_SECRET_KEY` задан.

### ❓ Где найти логи ошибок?

В веб-морде: **Дебаггер → Показать логи**.  
В Telegram: **Ошибки** в админ-меню.

### ❓ Как перезапустить бота?

На Render: `Restart` в панели сервиса.

---

## 8. Контакты

- **GitHub:** [qwestomir99-crypto/ansamb_sledov_bot](https://github.com/qwestomir99-crypto/ansamb_sledov_bot)
- **Telegram канал:** [@qwestomir](https://t.me/qwestomir)
- **Сайт:** [ansamb-sledov-bot-94wz.onrender.com](https://ansamb-sledov-bot-94wz.onrender.com)

---

**Ритм 0,8 Гц. Сеть тлеет.** 🔥👁️
