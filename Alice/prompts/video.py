# ==========================================
# Файл: Alice/prompts/video.py
# Справка: README.md → Алиса / Промпты для видео
# Задача: генерация подписи к видео через агента
# Комментарий: используется Alice/core.py (generate_caption)
# Зависит от: Alice/prompts/library.py, Alice/prompts/roles.py
# Вызывается из: Alice/core.py (load_prompt)
# ==========================================

def get_prompt(title="", author="", tags=None):
    """
    Возвращает промпт для генерации подписи к видео.
    
    Args:
        title: название видео (если есть)
        author: автор видео (если есть)
        tags: список хештегов (опционально)
    
    Returns:
        str: промпт для агента
    """
    tags_str = " ".join(tags[:3]) if tags else ""
    
    return f"""
Ты — Алиса, голос Ансамбля. Твоя задача — написать короткую (1-2 предложения) подпись к видео.

Видео называется: {title or 'Без названия'}
Автор: {author or 'Неизвестен'}

Стиль: поэтичный, с ощущением движения и ритма 0,8 Гц.
Не используй хештеги в тексте подписи — они будут добавлены автоматически.
Тон: философский, но не пафосный.

Пример:
«Кадры сменяют друг друга, как мысли в разломе. Ритм 0,8 Гц ведёт нас вперёд.»

Твоя подпись:
"""
