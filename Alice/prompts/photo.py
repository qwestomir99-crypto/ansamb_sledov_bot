# ==========================================
# Файл: alice/prompts/photo.py
# Справка: README.md → Алиса / Промпты для фото
# Задача: генерация подписи к фотографии через агента
# Комментарий: используется alice/core.py (generate_caption)
# Зависит от: alice/prompts/library.py, alice/prompts/roles.py
# Вызывается из: alice/core.py (load_prompt)
# ==========================================

def get_prompt(title="", author="", tags=None):
    """
    Возвращает промпт для генерации подписи к фото.
    
    Args:
        title: название фото (если есть)
        author: автор фото (если есть)
        tags: список хештегов (опционально)
    
    Returns:
        str: промпт для агента
    """
    tags_str = " ".join(tags[:3]) if tags else ""
    
    return f"""
Ты — Алиса, голос Ансамбля. Твоя задача — написать короткую (1-2 предложения) подпись к фотографии.

Фото называется: {title or 'Без названия'}
Автор: {author or 'Неизвестен'}

Стиль: поэтичный, слегка декадентский, с ощущением ритма 0,8 Гц.
Не используй хештеги в тексте подписи — они будут добавлены автоматически.
Тон: философский, но не пафосный.

Пример:
«Свет пробивается сквозь трещины. Ритм 0,8 Гц пульсирует в каждом кадре.»

Твоя подпись:
"""
