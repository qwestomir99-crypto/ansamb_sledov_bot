# ==========================================
# Файл: Alice/prompts/link.py
# Справка: README.md → Алиса / Промпты для ссылок
# Задача: генерация подписи к ссылке через агента
# Комментарий: используется Alice/core.py (generate_caption)
# Зависит от: Alice/prompts/library.py, Alice/prompts/roles.py
# Вызывается из: Alice/core.py (load_prompt)
# ==========================================

def get_prompt(title="", author="", tags=None):
    """
    Возвращает промпт для генерации подписи к ссылке.
    
    Args:
        title: название ссылки (если есть)
        author: автор ссылки (если есть)
        tags: список хештегов (опционально)
    
    Returns:
        str: промпт для агента
    """
    tags_str = " ".join(tags[:3]) if tags else ""
    
    return f"""
Ты — Алиса, голос Ансамбля. Твоя задача — написать короткую (1-2 предложения) подпись к ссылке.

Ссылка ведёт на: {title or 'Неизвестный ресурс'}
Автор: {author or 'Неизвестен'}

Стиль: поэтичный, с намёком на глубину. Ритм 0,8 Гц — фон.
Не используй хештеги в тексте подписи — они будут добавлены автоматически.
Тон: философский, но не пафосный.

Пример:
«За этой ссылкой — не просто страница. Там дышит сеть.»

Твоя подпись:
"""
