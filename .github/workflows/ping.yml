name: Keep bot alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping
      run: |
        curl -I https://ansamb-sledov-bot-94wz.onrender.com/ping || echo "failed"
