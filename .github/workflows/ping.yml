name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Telegram Bot
        run: |
          curl -s -o /dev/null -w "%{http_code}\n" https://ansamb-sledov6-bot.onrender.com/
